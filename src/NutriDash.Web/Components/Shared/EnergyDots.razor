<span class="energy-dots">
    @for (int i = 1; i <= 5; i++)
    {
        <span class="dot-e @(i <= Value ? "dot-e-filled" : "")"></span>
    }
</span>

@code {
    [Parameter] public int Value { get; set; }
}
